<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SCPApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>

        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/gray</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.SCPApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/scp_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.SCPApp</item>
    </style>
</resources>
